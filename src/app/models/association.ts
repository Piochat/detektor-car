export interface Association {
    associationId: number,
    vehicleId: number,
    ownerId: number
}

export interface AssociationDto {
    associationId: number,
    vehicleId: number,
    ownerId: number
}